import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  button: {
    cursor: "pointer"
  },
  brand: {
    fontSize: 26,
    fontWeight: "bold"
  },
  firstName: {
    color: "white",
  },
  lastName: {
  }
}));

export default useStyles;
